[build]
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

# Route API calls to serverless functions.
[[redirects]]
  from = "/api/auth"
  to = "/.netlify/functions/auth"
  status = 200
  force = true

[[redirects]]
  from = "/api/publish-image-chunk"
  to = "/.netlify/functions/publish-image-chunk"
  status = 200
  force = true

[[redirects]]
  from = "/api/publish-finalize"
  to = "/.netlify/functions/publish-finalize"
  status = 200
  force = true

# SPA fallback — send all unmatched routes to index.html for React Router.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
